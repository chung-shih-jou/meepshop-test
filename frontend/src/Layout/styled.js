import styled from 'styled-components';

export const Wrapper = styled.section`
    .content {
        padding: 16px;
        height: calc(100vh - 200px);
        .droppable-card,
        .droppable-card {
            height: 100%;
            width: 100%;
        }
    }
`;
